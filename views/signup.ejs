<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Masuk ke Twitter</title>
    <link href="/styles/signup.css" rel="stylesheet" type="text/css" />
  </head>

  <body>
    <div class="form">
      <img
        class="gambar2"
        style="display: block; margin: auto"
        src="/images/580b57fcd9996e24bc43c53e.png"
      />
      <h1>Buat akun</h1>

      <form action="/register" method="POST" autocomplete="off">
        <table>
          <tr>
            <td width="100" height="50">
              <input name="username" type="text" placeholder="nama" />
            </td>
          </tr>
          <tr>
            <td width="100" height="50">
              <input name="password" type="password" placeholder="Password" />
            </td>
          </tr>
        </table>

        <p><a href="#">Gunakan email</a></p>

        <div class="tengah">
          <b>Tanggal lahir</b>
          <p>
            ini tidak akan ditampilkan secara publik. Konfirmasikan usia anda
            sendiri, bahkan jika akun ini digunakan untuk bisnis, hewan
            peliharaan dan lainnya.
          </p>
        </div>

        <div class="kalender">
          <select name="bulan" id="bulan">
            <% for (let bulan of Month) { %>
            <option value="<%= bulan %>"><%= bulan %></option>
            <% } %>
          </select>
          <select name="tanggal" id="tanggal">
            <% for ( let i = 1; i <= 31; i++) { %>
            <option value="<%= i %>"><%= i %></option>
            <% } %>
          </select>

          <select name="tahun" id="tahun">
            <% for (let i = 1997; i <= 2004; i++) { %>
            <option value="<%= i %>"><%= i %></option>
            <% } %>
          </select>
        </div>

        <br />

        <button>selanjutnya</button>
      </form>
    </div>
  </body>
</html>
